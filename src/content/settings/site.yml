# The file() loader treats each top-level key as one entry id, so everything
# lives under a single `site:` key. In the CMS this is one "object" field
# named `site` — keep the two in sync if you rename anything.
site:
  phone: "303-555-0100"
  email: "eric@denvercustomcounseling.com"
  bookingUrl: "https://example.com/book"
  serviceArea: "Serving clients throughout the Denver metro area and Colorado, online only"
  telehealthDisclaimer: "This form is not monitored for emergencies. If you are in crisis, call or text 988 (Suicide & Crisis Lifeline) or 911."
